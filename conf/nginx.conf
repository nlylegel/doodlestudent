events{}

http {
   server {

      listen 80;
      
      location / {
        
        proxy_pass http://localhost:4200;
      }

      location /backend {
        rewrite ^/backend(.*)$ $1 break;
        proxy_pass http://localhost:5000;
      }
   }
}